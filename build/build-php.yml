# Purpose:
#   Install composer dependencies
#
# Configuration:
#   Adjust the needed artifact paths
#
# @deprecation this file is kept for backwards compatibility and will be removed in the future.
# It's highly recommended to avoid including this file but instead include one of the more specific files like build-php-*.yaml.
#

build:php:
  extends: .build-php
