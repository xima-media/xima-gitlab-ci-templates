analyse:php:stan:
  stage: analyse
  needs: [ "build:php" ]
  script:
    - composer run php:stan:check

analyse:php:rector:
  stage: analyse
  needs: [ "build:php" ]
  script:
    - composer run migration:rector:check
